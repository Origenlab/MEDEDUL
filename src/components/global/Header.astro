---
// Header Component - Professional Navigation v4 (robust click + hover + mobile)
import { getCdnUrl } from '@/lib/cdn';

interface Props {
  currentPath?: string;
}

const { currentPath = '/' } = Astro.props;
const logoPath = '/img/mededul.png';
const logoSrc = getCdnUrl(logoPath, { w: 240, q: 80 });
const logoSrcSet = [
  `${getCdnUrl(logoPath, { w: 120, q: 80 })} 120w`,
  `${getCdnUrl(logoPath, { w: 240, q: 80 })} 240w`,
  `${getCdnUrl(logoPath, { w: 360, q: 80 })} 360w`,
  `${getCdnUrl(logoPath, { w: 480, q: 80 })} 480w`
].join(', ');

// Sucursales para el bloque de contacto del header
const sucursales = [
  {
    id: 'cdmx',
    city: 'CDMX',
    cityFull: 'Ciudad de Mexico',
    number: '55 2522 6442',
    tel: 'tel:+525525226442',
    whatsapp: 'https://wa.me/525525226442?text=Hola%20MEDEDUL%2C%20me%20interesa%20cotizar%20una%20mesa%20de%20dulces',
    horario: 'Lun-Sab 10:00-19:00 h',
    zona: 'Anzures & Condesa'
  },
  {
    id: 'toluca',
    city: 'Toluca',
    cityFull: 'Toluca / Metepec',
    number: '55 2177 5184',
    tel: 'tel:+525521775184',
    whatsapp: 'https://wa.me/525521775184?text=Hola%20MEDEDUL%2C%20me%20interesa%20cotizar%20una%20mesa%20de%20dulces%20en%20Toluca%2FMetepec',
    horario: 'Lun-Sab 10:00-19:00 h',
    zona: 'Metepec'
  }
];

// Navigation structure — Directorio movido al footer
const menuItems = [
  { label: 'Inicio', href: '/', exact: true },
  {
    label: 'Servicios',
    href: '/servicios',
    megaMenu: [
      {
        title: 'Tipos de Mesas',
        icon: '🍬',
        color: '#D1007A',
        items: [
          { label: 'Mesa de Dulces', href: '/tipos-de-mesas-de-dulces/mesa-de-dulces' },
          { label: 'Mesa de Frutas', href: '/tipos-de-mesas-de-dulces/mesa-de-frutas' },
          { label: 'Mesa de Postres', href: '/tipos-de-mesas-de-dulces/mesa-de-postres' },
          { label: 'Quesos y Embutidos', href: '/tipos-de-mesas-de-dulces/mesa-de-quesos-y-embutidos' },
          { label: 'Mesa de Snacks', href: '/tipos-de-mesas-de-dulces/mesa-de-snacks' },
          { label: 'Mesa de Botanas', href: '/tipos-de-mesas-de-dulces/mesa-de-botanas' },
          { label: 'Dulces Tradicionales', href: '/tipos-de-mesas-de-dulces/mesa-de-dulces-tradicionales' },
          { label: 'Donas y Cupcakes', href: '/tipos-de-mesas-de-dulces/mesa-de-donas-y-cupcakes' }
        ],
        viewAll: { label: 'Ver todos los tipos', href: '/tipos-de-mesas-de-dulces' }
      },
      {
        title: 'Candy Bar por Evento',
        icon: '🎉',
        color: '#A0005F',
        items: [
          { label: 'Mesas Temáticas', href: '/candy-bar-eventos/mesas-tematicas' },
          { label: 'Bodas', href: '/candy-bar-eventos/mesa-dulces-boda' },
          { label: 'XV Años', href: '/candy-bar-eventos/mesa-dulces-xv-anos' },
          { label: 'Baby Shower', href: '/candy-bar-eventos/mesa-dulces-baby-shower' },
          { label: 'Bautizos', href: '/candy-bar-eventos/mesa-dulces-bautizo' },
          { label: 'Primera Comunión', href: '/candy-bar-eventos/mesa-dulces-primera-comunion' },
          { label: 'Cumpleaños Infantil', href: '/candy-bar-eventos/mesa-dulces-cumpleanos-infantil' },
          { label: 'Graduaciones', href: '/candy-bar-eventos/mesa-dulces-graduacion' },
          { label: 'Corporativos', href: '/candy-bar-eventos/mesa-dulces-corporativos' }
        ],
        viewAll: { label: 'Ver todos los eventos', href: '/candy-bar-eventos' }
      },
      {
        title: 'Estaciones Interactivas',
        icon: '⭐',
        color: '#7A2BC2',
        items: [
          { label: 'Fuente de Chocolate', href: '/estaciones-interactivas/fuente-de-chocolate' },
          { label: 'Fuente de Chamoy', href: '/estaciones-interactivas/fuente-de-chamoy' },
          { label: 'Pared de Dulces', href: '/estaciones-interactivas/pared-de-dulces' },
          { label: 'Stand para Pastel', href: '/estaciones-interactivas/stand-para-pastel' }
        ],
        viewAll: { label: 'Ver todas las estaciones', href: '/estaciones-interactivas' }
      },
      {
        title: 'Por qué Elegirnos',
        icon: '💎',
        color: '#3A0A74',
        items: [
          { label: 'Diseño Personalizado', href: '/porque-mededul/diseno-personalizado' },
          { label: 'Calidad Premium', href: '/porque-mededul/calidad-premium' },
          { label: 'Servicio Completo', href: '/porque-mededul/servicio-completo' },
          { label: 'Cero Estrés', href: '/porque-mededul/cero-estres' }
        ],
        viewAll: { label: 'Conoce más', href: '/porque-mededul' }
      }
    ]
  },
  {
    label: 'Sucursal CDMX',
    href: '/sucursales/cdmx',
    startsWith: true,
    megaMenu: [
      {
        title: 'Servicios CDMX',
        icon: '🍬',
        color: '#D1007A',
        items: [
          { label: 'Mesa de Dulces CDMX', href: '/sucursales/cdmx/mesa-de-dulces' },
          { label: 'Mesa de Frutas CDMX', href: '/sucursales/cdmx/mesa-de-frutas' },
          { label: 'Mesa de Postres CDMX', href: '/sucursales/cdmx/mesa-de-postres' },
          { label: 'Quesos y Embutidos CDMX', href: '/sucursales/cdmx/mesa-de-quesos-y-embutidos' }
        ],
        viewAll: { label: 'Hub Sucursal CDMX', href: '/sucursales/cdmx' }
      },
      {
        title: 'CDMX por Evento',
        icon: '🎉',
        color: '#A0005F',
        items: [
          { label: 'Candy Bar Boda CDMX', href: '/sucursales/cdmx/mesa-de-dulces/candy-bar-boda' },
          { label: 'Mesa XV Años CDMX', href: '/sucursales/cdmx/mesa-de-dulces/mesa-xv-anos' },
          { label: 'Baby Shower CDMX', href: '/sucursales/cdmx/mesa-de-dulces/baby-shower' },
          { label: 'Cumpleaños Infantil CDMX', href: '/sucursales/cdmx/mesa-de-dulces/cumpleanos-infantil' }
        ],
        viewAll: { label: 'Ver mesa-de-dulces CDMX', href: '/sucursales/cdmx/mesa-de-dulces' }
      },
      {
        title: 'CDMX por Zona',
        icon: '📍',
        color: '#7A2BC2',
        items: [
          { label: 'Polanco', href: '/zonas-cdmx/polanco' },
          { label: 'Santa Fe', href: '/zonas-cdmx/santa-fe' },
          { label: 'Interlomas', href: '/zonas-cdmx/interlomas' },
          { label: 'Condesa', href: '/zonas-cdmx/condesa' },
          { label: 'Roma Norte', href: '/zonas-cdmx/roma-norte' },
          { label: 'Coyoacán', href: '/zonas-cdmx/coyoacan' },
          { label: 'Del Valle', href: '/zonas-cdmx/del-valle' },
          { label: 'Anzures · Narvarte · más', href: '/zonas-cdmx' }
        ],
        viewAll: { label: 'Cobertura CDMX (16 alcaldías)', href: '/zonas-cdmx' }
      },
      {
        title: 'Recursos CDMX',
        icon: '📞',
        color: '#3A0A74',
        items: [
          { label: 'Cotizar mesa CDMX', href: '/cotizar' },
          { label: 'Galería de eventos', href: '/galeria' },
          { label: 'Testimonios', href: '/testimonios' },
          { label: 'Precios y paquetes', href: '/precios' },
          { label: 'Cómo trabajamos', href: '/proceso' }
        ],
        viewAll: { label: 'WhatsApp CDMX', href: 'https://wa.me/525525226442' }
      }
    ]
  },
  {
    label: 'Sucursal Toluca',
    href: '/sucursales/toluca',
    startsWith: false,
    megaMenu: [
      {
        title: 'Sucursal Toluca-Metepec',
        icon: '🏞️',
        color: '#7A2BC2',
        items: [
          { label: 'Hub Sucursal Toluca', href: '/sucursales/toluca' },
          { label: 'Mesa de Dulces Toluca', href: '/sucursales/toluca/mesa-de-dulces' },
          { label: '↳ Candy Bar para Boda Toluca', href: '/sucursales/toluca/mesa-de-dulces/candy-bar-boda' },
          { label: '↳ Mesa para XV Años Toluca', href: '/sucursales/toluca/mesa-de-dulces/mesa-xv-anos' },
          { label: '↳ Baby Shower Toluca', href: '/sucursales/toluca/mesa-de-dulces/baby-shower' },
          { label: '↳ Cumpleaños Infantil Toluca', href: '/sucursales/toluca/mesa-de-dulces/cumpleanos-infantil' },
          { label: 'Mesa de Frutas Toluca', href: '/sucursales/toluca/mesa-de-frutas' },
          { label: 'Mesa de Postres Toluca', href: '/sucursales/toluca/mesa-de-postres' },
          { label: 'Quesos y Embutidos Toluca', href: '/sucursales/toluca/mesa-de-quesos-y-embutidos' },
          { label: '↳ Quesos Europeos Toluca', href: '/sucursales/toluca/mesa-de-quesos-y-embutidos/quesos-europeos' },
          { label: '↳ Jamones Curados Toluca', href: '/sucursales/toluca/mesa-de-quesos-y-embutidos/jamones-curados' },
          { label: '↳ Tabla Mixta Premium Toluca', href: '/sucursales/toluca/mesa-de-quesos-y-embutidos/tabla-mixta-premium' },
          { label: 'Mesa de Snacks Toluca', href: '/sucursales/toluca/mesa-de-snacks' },
          { label: '↳ Snacks Salados Toluca', href: '/sucursales/toluca/mesa-de-snacks/snacks-gourmet-salados' },
          { label: '↳ Mix Dulce-Salado Toluca', href: '/sucursales/toluca/mesa-de-snacks/mix-dulce-salado' },
          { label: '↳ Barra Infantil Toluca', href: '/sucursales/toluca/mesa-de-snacks/barra-infantil' },
          { label: 'Mesas de Dulces Metepec', href: '/mesas-de-dulces-metepec' },
          { label: 'Mesas de Dulces Toluca', href: '/mesas-de-dulces-toluca' },
          { label: 'Mesas de Dulces Zinacantepec', href: '/mesas-de-dulces-zinacantepec' },
          { label: 'Mesas de Dulces Lerma', href: '/mesas-de-dulces-lerma' }
        ],
        viewAll: { label: 'Ver sucursal Toluca', href: '/sucursales/toluca' }
      },
      {
        title: 'Cobertura',
        icon: '📍',
        color: '#A0005F',
        items: [
          { label: 'Valle de Toluca completo', href: '/sucursales/toluca#zonas-toluca' },
          { label: 'Ver todas las sucursales', href: '/sucursales' },
          { label: 'Cobertura nacional', href: '/sucursales' }
        ],
        viewAll: { label: 'WhatsApp Toluca', href: 'https://wa.me/525521775184' }
      }
    ]
  },
  { label: 'Blog', href: '/blog', startsWith: true },
  { label: 'Contacto', href: '/contacto' }
];

// Check if link is active
const isActive = (item: typeof menuItems[0]) => {
  if (item.exact) return currentPath === item.href;
  if (item.startsWith) return currentPath.startsWith(item.href);
  return currentPath === item.href;
};
---

<header class="header" id="header">
  <div class="header__container">
    <!-- Logo -->
    <a href="/" class="header__logo" aria-label="Mededul — inicio">
      <img
        src={logoSrc}
        srcset={logoSrcSet}
        sizes="240px"
        alt="Mededul - Mesas de Dulces CDMX, Toluca y Metepec"
        width="240"
        height="60"
        loading="eager"
        decoding="async"
        fetchpriority="high"
      />
    </a>

    <!-- Desktop Navigation -->
    <nav class="header__nav" id="nav-menu" aria-label="Navegación principal">
      <ul class="header__menu">
        {menuItems.map((item, idx) => (
          <li class:list={['header__item', { 'has-mega': item.megaMenu }]} data-mega-id={item.megaMenu ? `mega-${idx}` : undefined}>
            <a
              href={item.href}
              class:list={['header__link', { active: isActive(item) }]}
            >
              <span class="header__link-text">{item.label}</span>
            </a>
            {item.megaMenu && (
              <button
                type="button"
                class="header__mega-toggle"
                aria-expanded="false"
                aria-controls={`mega-${idx}`}
                aria-label={`Abrir submenú de ${item.label}`}
              >
                <svg class="header__arrow" width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true">
                  <polyline points="6 9 12 15 18 9" />
                </svg>
              </button>
            )}

            {item.megaMenu && (
              <div class="mega-menu" id={`mega-${idx}`} role="region" aria-label={`Submenú de ${item.label}`}>
                <div class="mega-menu__container">
                  {item.megaMenu.map(column => (
                    <div class="mega-menu__column">
                      <div class="mega-menu__header" style={`--column-color: ${column.color}`}>
                        <span class="mega-menu__icon" aria-hidden="true">{column.icon}</span>
                        <span class="mega-menu__title">{column.title}</span>
                      </div>
                      <ul class="mega-menu__list">
                        {column.items.map(subItem => (
                          <li>
                            <a href={subItem.href} class="mega-menu__link">
                              {subItem.label}
                            </a>
                          </li>
                        ))}
                      </ul>
                      <a href={column.viewAll.href} class="mega-menu__view-all" style={`--column-color: ${column.color}`}>
                        {column.viewAll.label}
                        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
                          <path d="M5 12h14M12 5l7 7-7 7"/>
                        </svg>
                      </a>
                    </div>
                  ))}
                </div>
              </div>
            )}
          </li>
        ))}
      </ul>
    </nav>

    <!-- Actions -->
    <div class="header__actions">
      <!-- Dual sucursal contact -->
      <div class="header__contact" aria-label="Contacto sucursales">
        {sucursales.map((s) => (
          <a href={s.tel} class="suc-chip" aria-label={`Llamar a sucursal ${s.cityFull} al ${s.number}`}>
            <span class="suc-chip__icon" aria-hidden="true">
              <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2">
                <path d="M22 16.92v3a2 2 0 01-2.18 2 19.79 19.79 0 01-8.63-3.07 19.5 19.5 0 01-6-6 19.79 19.79 0 01-3.07-8.67A2 2 0 014.11 2h3a2 2 0 012 1.72 12.84 12.84 0 00.7 2.81 2 2 0 01-.45 2.11L8.09 9.91a16 16 0 006 6l1.27-1.27a2 2 0 012.11-.45 12.84 12.84 0 002.81.7A2 2 0 0122 16.92z"/>
              </svg>
            </span>
            <span class="suc-chip__body">
              <span class="suc-chip__city">{s.city}</span>
              <span class="suc-chip__num">{s.number}</span>
            </span>
          </a>
        ))}
      </div>

      <a href="/cotizar" class="header__cta">
        <span>Cotizar</span>
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" aria-hidden="true">
          <path d="M5 12h14M12 5l7 7-7 7"/>
        </svg>
      </a>
    </div>

    <!-- Mobile Toggle -->
    <button class="header__toggle" id="hamburger" type="button" aria-label="Abrir menú" aria-expanded="false" aria-controls="mobile-menu">
      <span></span>
      <span></span>
      <span></span>
    </button>
  </div>
</header>

<!-- Mobile Menu (FUERA del <header> porque el backdrop-filter del header crea un
     containing block que rompe position:fixed de los hijos) -->
<div class="mobile-menu" id="mobile-menu" aria-hidden="true">
    <div class="mobile-menu__inner">
      <ul class="mobile-menu__list">
        {menuItems.map(item => (
          <li class:list={['mobile-menu__item', { 'has-submenu': item.megaMenu }]}>
            {item.megaMenu ? (
              <>
                <div class="mobile-menu__row">
                  <a href={item.href} class:list={['mobile-menu__link', 'mobile-menu__link--main', { active: isActive(item) }]}>
                    {item.label}
                  </a>
                  <button class="mobile-menu__trigger" type="button" aria-expanded="false" aria-label={`Mostrar submenú de ${item.label}`}>
                    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
                      <polyline points="6 9 12 15 18 9" />
                    </svg>
                  </button>
                </div>
                <div class="mobile-menu__submenu">
                  {item.megaMenu.map(column => (
                    <div class="mobile-menu__group">
                      <button class="mobile-menu__group-header" type="button" aria-expanded="false" style={`--column-color: ${column.color}`}>
                        <span class="mobile-menu__group-icon">{column.icon}</span>
                        <span class="mobile-menu__group-title">{column.title}</span>
                        <svg width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
                          <polyline points="6 9 12 15 18 9" />
                        </svg>
                      </button>
                      <ul class="mobile-menu__group-list">
                        {column.items.map(subItem => (
                          <li>
                            <a href={subItem.href} class="mobile-menu__sublink">{subItem.label}</a>
                          </li>
                        ))}
                        <li>
                          <a href={column.viewAll.href} class="mobile-menu__view-all">{column.viewAll.label}</a>
                        </li>
                      </ul>
                    </div>
                  ))}
                </div>
              </>
            ) : (
              <a href={item.href} class:list={['mobile-menu__link', { active: isActive(item) }]}>
                {item.label}
              </a>
            )}
          </li>
        ))}
      </ul>

    </div>
</div>

<script is:inline>
  (function () {
    if (window.__mededulHeaderInit) return;
    window.__mededulHeaderInit = true;

    const ready = (fn) => {
      if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', fn, { once: true });
      } else {
        fn();
      }
    };

    ready(function init() {
      const header = document.getElementById('header');
      const hamburger = document.getElementById('hamburger');
      const mobileMenu = document.getElementById('mobile-menu');
      if (!header) return;

      const isDesktop = () => window.matchMedia('(min-width: 1025px)').matches;

      /* ====== Scroll effect ====== */
      let lastScroll = -1;
      const onScroll = () => {
        const cur = window.scrollY;
        if (cur === lastScroll) return;
        lastScroll = cur;
        if (cur > 50) header.classList.add('scrolled');
        else header.classList.remove('scrolled');
      };
      window.addEventListener('scroll', onScroll, { passive: true });
      onScroll();

      /* ====== Mobile menu open/close ====== */
      const closeMobile = () => {
        if (!hamburger || !mobileMenu) return;
        hamburger.classList.remove('active');
        mobileMenu.classList.remove('active');
        mobileMenu.setAttribute('aria-hidden', 'true');
        document.body.classList.remove('menu-open');
        hamburger.setAttribute('aria-expanded', 'false');
        hamburger.setAttribute('aria-label', 'Abrir menú');
      };

      const openMobile = () => {
        if (!hamburger || !mobileMenu) return;
        hamburger.classList.add('active');
        mobileMenu.classList.add('active');
        mobileMenu.setAttribute('aria-hidden', 'false');
        document.body.classList.add('menu-open');
        hamburger.setAttribute('aria-expanded', 'true');
        hamburger.setAttribute('aria-label', 'Cerrar menú');
      };

      if (hamburger && mobileMenu) {
        hamburger.addEventListener('click', (e) => {
          e.preventDefault();
          e.stopPropagation();
          if (hamburger.classList.contains('active')) closeMobile();
          else openMobile();
        });
      }

      /* ====== Mobile menu inner interactions ====== */
      if (mobileMenu) {
        mobileMenu.addEventListener('click', (e) => {
          const t = e.target;
          if (!(t instanceof Element)) return;

          // top-level submenu toggle (Servicios, Sucursales)
          const trigger = t.closest('.mobile-menu__trigger');
          if (trigger) {
            e.preventDefault();
            const item = trigger.closest('.mobile-menu__item');
            if (!item) return;
            const willOpen = !item.classList.contains('open');
            // Close other top-level submenus
            mobileMenu.querySelectorAll('.mobile-menu__item.open').forEach((el) => {
              if (el !== item) {
                el.classList.remove('open');
                const otherTrig = el.querySelector('.mobile-menu__trigger');
                if (otherTrig) otherTrig.setAttribute('aria-expanded', 'false');
              }
            });
            item.classList.toggle('open', willOpen);
            trigger.setAttribute('aria-expanded', willOpen ? 'true' : 'false');
            return;
          }

          // group header toggle (Tipos de Mesas, Candy Bar, ...)
          const groupHead = t.closest('.mobile-menu__group-header');
          if (groupHead) {
            e.preventDefault();
            const group = groupHead.parentElement;
            if (!group) return;
            const willOpen = !group.classList.contains('open');
            group.classList.toggle('open', willOpen);
            groupHead.setAttribute('aria-expanded', willOpen ? 'true' : 'false');
            return;
          }

          // any navigation link closes the menu
          const link = t.closest(
            'a.mobile-menu__sublink, a.mobile-menu__view-all, a.mobile-menu__link'
          );
          if (link) closeMobile();
        });
      }

      /* ====== Desktop mega menu: hover-intent JS + click toggle ====== */
      const megaItems = header.querySelectorAll('.header__item.has-mega');
      const closeTimers = new WeakMap();

      const closeItem = (it) => {
        it.classList.remove('open');
        const btn = it.querySelector('.header__mega-toggle');
        if (btn) btn.setAttribute('aria-expanded', 'false');
      };

      const closeAllMega = () => {
        megaItems.forEach((it) => {
          const t = closeTimers.get(it);
          if (t) { clearTimeout(t); closeTimers.delete(it); }
          closeItem(it);
        });
      };

      const openItem = (it) => {
        const t = closeTimers.get(it);
        if (t) { clearTimeout(t); closeTimers.delete(it); }
        megaItems.forEach((other) => { if (other !== it) closeItem(other); });
        it.classList.add('open');
        const btn = it.querySelector('.header__mega-toggle');
        if (btn) btn.setAttribute('aria-expanded', 'true');
      };

      const scheduleClose = (it, delay = 220) => {
        const prev = closeTimers.get(it);
        if (prev) clearTimeout(prev);
        const t = setTimeout(() => {
          closeItem(it);
          closeTimers.delete(it);
        }, delay);
        closeTimers.set(it, t);
      };

      megaItems.forEach((item) => {
        // Hover-intent: abrir al entrar, cerrar con delay al salir
        item.addEventListener('mouseenter', () => {
          if (!isDesktop()) return;
          openItem(item);
        });
        item.addEventListener('mouseleave', () => {
          if (!isDesktop()) return;
          scheduleClose(item);
        });

        // Click en la flecha = toggle (cubre teclado, touchscreen, trackpad lento)
        const btn = item.querySelector('.header__mega-toggle');
        if (btn) {
          btn.addEventListener('click', (e) => {
            e.preventDefault();
            e.stopPropagation();
            if (item.classList.contains('open')) closeItem(item);
            else openItem(item);
          });
        }
      });

      // Click fuera del header cierra todos
      document.addEventListener('click', (e) => {
        const t = e.target;
        if (!(t instanceof Element)) return;
        if (!t.closest('.header__item.has-mega')) closeAllMega();
      });

      /* ====== Escape closes everything ====== */
      document.addEventListener('keydown', (e) => {
        if (e.key !== 'Escape') return;
        if (mobileMenu && mobileMenu.classList.contains('active')) closeMobile();
        closeAllMega();
      });

      /* ====== Resize: close mobile on desktop, close mega on mobile ====== */
      let resizeRaf;
      window.addEventListener('resize', () => {
        if (resizeRaf) cancelAnimationFrame(resizeRaf);
        resizeRaf = requestAnimationFrame(() => {
          if (isDesktop() && mobileMenu && mobileMenu.classList.contains('active')) {
            closeMobile();
          }
          if (!isDesktop()) {
            closeAllMega();
          }
        });
      }, { passive: true });
    });
  })();
</script>

<style>
  /* =====================================================
     HEADER v4 — Premium con click+hover robusto
     ===================================================== */
  .header {
    position: fixed;
    top: var(--topbar-offset, 44px);
    left: 0;
    right: 0;
    z-index: 1000;
    background: rgba(255, 255, 255, 0.94);
    backdrop-filter: saturate(180%) blur(16px);
    -webkit-backdrop-filter: saturate(180%) blur(16px);
    border-top: 1px solid rgba(15, 15, 30, 0.04);
    border-bottom: 1px solid rgba(15, 15, 30, 0.06);
    box-shadow: 0 1px 0 rgba(255,255,255,0.7) inset;
    transition: box-shadow .25s ease, background .25s ease, border-color .25s ease;
  }

  .header.scrolled {
    background: rgba(255, 255, 255, 0.985);
    box-shadow: 0 8px 28px rgba(15, 15, 30, 0.08);
    border-bottom-color: rgba(15, 15, 30, 0.08);
  }

  .header__container {
    max-width: 1380px;
    margin: 0 auto;
    padding: 12px 32px;
    min-height: 76px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 20px;
  }

  /* ---------- Logo ---------- */
  .header__logo {
    flex-shrink: 0;
    display: flex;
    align-items: center;
    line-height: 0;
    transition: transform .25s ease, opacity .2s ease;
  }
  .header__logo:hover { opacity: .9; transform: scale(1.02); }

  .header__logo img {
    height: 54px;
    width: auto;
    max-width: 240px;
    display: block;
    object-fit: contain;
  }

  /* ---------- Navigation ---------- */
  .header__nav {
    flex: 1;
    display: flex;
    justify-content: center;
  }

  .header__menu {
    display: flex;
    align-items: center;
    gap: 2px;
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .header__item { position: relative; display: flex; align-items: center; }

  .header__link {
    position: relative;
    display: flex;
    align-items: center;
    padding: 11px 14px;
    font-size: 0.92rem;
    font-weight: 500;
    color: #2a2a35;
    text-decoration: none;
    border-radius: 8px;
    letter-spacing: 0.01em;
    transition: color .15s ease, background-color .15s ease;
  }

  .header__item.has-mega > .header__link { padding-right: 4px; }

  .header__link::before {
    content: '';
    position: absolute;
    left: 14px;
    right: 14px;
    bottom: 4px;
    height: 2px;
    background: linear-gradient(90deg, #D1007A, #7A2BC2);
    border-radius: 2px;
    opacity: 0;
    transition: opacity .15s ease;
  }

  .header__link:hover { color: #D1007A; }
  .header__link:hover::before { opacity: 1; }

  .header__link.active { color: #D1007A; }
  .header__link.active::before { opacity: 1; }

  .header__link:focus-visible {
    outline: 2px solid #D1007A;
    outline-offset: 3px;
  }

  /* Botón flecha del mega-menu */
  .header__mega-toggle {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 26px;
    height: 26px;
    margin-left: 1px;
    padding: 0;
    background: transparent;
    border: 0;
    border-radius: 6px;
    color: #2a2a35;
    cursor: pointer;
    transition: background .2s ease, color .2s ease;
  }
  .header__mega-toggle:hover { background: rgba(209, 0, 122, 0.10); color: #D1007A; }
  .header__mega-toggle:focus-visible {
    outline: 2px solid #D1007A;
    outline-offset: 2px;
  }
  .header__mega-toggle .header__arrow {
    opacity: .65;
    transition: opacity .15s ease;
  }
  .header__item.has-mega.open > .header__mega-toggle .header__arrow { opacity: 1; }
  .header__item.has-mega.open > .header__mega-toggle { background: rgba(209, 0, 122, 0.10); color: #D1007A; }

  /* ---------- Mega Menu (sin movimientos: solo opacidad + color) ---------- */
  .mega-menu {
    position: absolute;
    top: 100%;
    left: 50%;
    /* !important: global.css aplica `*:hover { transform: none !important }` que rompe el centrado */
    transform: translateX(-50%) !important;
    padding-top: 14px;
    opacity: 0;
    visibility: hidden;
    transition: opacity .18s ease, visibility 0s linear .18s;
    pointer-events: none;
    z-index: 1001;
  }

  .header__item.has-mega:focus-within > .mega-menu,
  .header__item.has-mega.open > .mega-menu {
    opacity: 1;
    visibility: visible;
    transition: opacity .18s ease, visibility 0s linear 0s;
    pointer-events: auto;
  }

  /* Refuerzo: centrado constante bajo cualquier estado */
  .mega-menu:hover,
  .mega-menu:focus-within,
  .mega-menu:active,
  .header__item.has-mega.open > .mega-menu:hover,
  .header__item.has-mega.open > .mega-menu:focus-within,
  .header__item.has-mega.open > .mega-menu:active {
    transform: translateX(-50%) !important;
  }

  .mega-menu__container {
    display: grid;
    grid-auto-flow: column;
    grid-auto-columns: 280px;
    gap: 0;
    background: #ffffff;
    border-radius: 14px;
    border: 1px solid rgba(15, 15, 30, 0.08);
    box-shadow:
      0 2px 6px rgba(15, 15, 30, 0.04),
      0 24px 56px rgba(15, 15, 30, 0.14);
    overflow: hidden;
    width: max-content;
    max-width: min(95vw, 1280px);
    position: relative;
  }
  /* Accent strip superior — refuerzo profesional */
  .mega-menu__container::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 3px;
    background: linear-gradient(90deg, #D1007A 0%, #A0005F 50%, #7A2BC2 100%);
  }

  .mega-menu__column {
    padding: 26px 20px 0;
    border-right: 1px solid #f0f0f4;
    background: #ffffff;
    display: flex;
    flex-direction: column;
    transition: background-color .15s ease;
  }
  .mega-menu__column:last-child { border-right: none; }
  .mega-menu__column:hover { background: #fbfbfd; }

  .mega-menu__header {
    display: flex;
    align-items: center;
    gap: 10px;
    margin-bottom: 16px;
    padding-bottom: 12px;
    border-bottom: 2px solid var(--column-color, #D1007A);
  }

  .mega-menu__icon {
    font-size: 1.05rem;
    line-height: 1;
    width: 30px;
    height: 30px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    background: color-mix(in srgb, var(--column-color, #D1007A) 12%, #fff);
    border-radius: 7px;
    flex-shrink: 0;
  }

  .mega-menu__title {
    font-size: 0.72rem;
    font-weight: 700;
    color: #0f0f1e;
    text-transform: uppercase;
    letter-spacing: 1.4px;
    white-space: nowrap;
  }

  .mega-menu__list {
    list-style: none;
    padding: 0;
    margin: 0 0 14px;
    flex: 1;
  }
  .mega-menu__list li { margin-bottom: 2px; }

  .mega-menu__link {
    display: flex;
    align-items: center;
    padding: 10px 12px;
    font-size: 0.875rem;
    font-weight: 500;
    color: #3d3d4a;
    text-decoration: none;
    border-radius: 8px;
    border-left: 2px solid transparent;
    white-space: nowrap;
    transition: color .15s ease, background-color .15s ease, border-color .15s ease;
  }
  .mega-menu__link:hover,
  .mega-menu__link:focus-visible {
    color: var(--column-color, #D1007A);
    background: color-mix(in srgb, var(--column-color, #D1007A) 7%, #fff);
    border-left-color: var(--column-color, #D1007A);
    outline: none;
  }

  .mega-menu__view-all {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 8px;
    font-size: 0.74rem;
    font-weight: 700;
    color: var(--column-color, #D1007A);
    text-decoration: none;
    padding: 14px 20px;
    margin-top: auto;
    border-top: 1px solid #f0f0f4;
    width: calc(100% + 40px);
    margin-left: -20px;
    background: #fbfbfd;
    text-transform: uppercase;
    letter-spacing: 0.7px;
    white-space: nowrap;
    transition: background-color .15s ease, color .15s ease;
  }
  .mega-menu__view-all:hover,
  .mega-menu__view-all:focus-visible {
    background: color-mix(in srgb, var(--column-color, #D1007A) 10%, #fff);
    outline: none;
  }
  .mega-menu__view-all svg { flex-shrink: 0; opacity: .8; }

  /* =====================================================
     ACTIONS — contacto dual + WhatsApp + CTA
     ===================================================== */
  .header__actions {
    display: flex;
    align-items: center;
    gap: 10px;
    flex-shrink: 0;
  }

  .header__contact {
    display: flex;
    align-items: stretch;
    gap: 0;
    padding: 4px;
    background: linear-gradient(135deg, rgba(209, 0, 122, 0.04), rgba(122, 43, 194, 0.03));
    border: 1px solid rgba(15, 15, 30, 0.06);
    border-radius: 12px;
  }

  .suc-chip {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 4px 10px 4px 6px;
    text-decoration: none;
    border-radius: 8px;
    transition: background .2s ease, transform .15s ease;
  }
  .suc-chip + .suc-chip { border-left: 1px solid rgba(15, 15, 30, 0.07); border-radius: 0 8px 8px 0; }
  .suc-chip:first-child { border-radius: 8px 0 0 8px; }
  .suc-chip:hover { background: rgba(255, 255, 255, 0.85); }
  .suc-chip:focus-visible {
    outline: 2px solid #D1007A;
    outline-offset: 2px;
  }

  .suc-chip__icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 26px;
    height: 26px;
    border-radius: 8px;
    background: linear-gradient(135deg, #D1007A 0%, #7A2BC2 100%);
    color: #ffffff;
    flex-shrink: 0;
    box-shadow: 0 2px 6px rgba(209, 0, 122, 0.25);
  }

  .suc-chip__body {
    display: flex;
    flex-direction: column;
    line-height: 1.1;
  }
  .suc-chip__city {
    font-size: 0.6rem;
    font-weight: 700;
    letter-spacing: 1.3px;
    text-transform: uppercase;
    color: #8b8b95;
  }
  .suc-chip__num {
    font-size: 0.86rem;
    font-weight: 700;
    color: #1a1a26;
    margin-top: 1px;
    font-variant-numeric: tabular-nums;
  }
  .suc-chip:hover .suc-chip__city { color: #D1007A; }

  /* CTA */
  .header__cta {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 11px 20px;
    font-size: 0.9rem;
    font-weight: 600;
    color: #ffffff;
    background: linear-gradient(135deg, #D1007A 0%, #A0005F 60%, #7A2BC2 100%);
    background-size: 200% 100%;
    background-position: 0% 50%;
    border-radius: 50px;
    text-decoration: none;
    box-shadow:
      0 1px 2px rgba(209, 0, 122, 0.25),
      0 6px 18px rgba(209, 0, 122, 0.28);
    transition: background-position .35s ease, box-shadow .25s ease, transform .15s ease;
    letter-spacing: 0.02em;
    white-space: nowrap;
  }
  .header__cta:hover {
    background-position: 100% 50%;
    box-shadow:
      0 1px 2px rgba(209, 0, 122, 0.3),
      0 10px 26px rgba(209, 0, 122, 0.38);
    transform: translateY(-1px);
  }
  .header__cta:active { transform: translateY(0); }
  .header__cta:focus-visible {
    outline: 2px solid #ffffff;
    outline-offset: 2px;
    box-shadow:
      0 0 0 4px rgba(209, 0, 122, 0.4),
      0 6px 18px rgba(209, 0, 122, 0.28);
  }
  .header__cta svg { transition: transform .2s ease; }
  .header__cta:hover svg { transform: translateX(3px); }

  /* ---------- Mobile Toggle ---------- */
  .header__toggle {
    display: none;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    gap: 5px;
    width: 44px;
    height: 44px;
    padding: 0;
    background: none;
    border: 1px solid rgba(15, 15, 30, 0.08);
    border-radius: 10px;
    cursor: pointer;
    transition: background .2s ease, border-color .2s ease;
    -webkit-tap-highlight-color: transparent;
    touch-action: manipulation;
  }
  .header__toggle:hover { background: rgba(15, 15, 30, 0.03); border-color: rgba(15, 15, 30, 0.14); }
  .header__toggle:active { background: rgba(15, 15, 30, 0.05); }
  .header__toggle:focus-visible {
    outline: 2px solid #D1007A;
    outline-offset: 2px;
  }

  .header__toggle span {
    display: block;
    width: 22px;
    height: 2px;
    background: #2a2a35;
    border-radius: 2px;
    transition: transform .25s ease, opacity .2s ease;
    pointer-events: none;
  }
  .header__toggle.active span:nth-child(1) { transform: translateY(7px) rotate(45deg); }
  .header__toggle.active span:nth-child(2) { opacity: 0; }
  .header__toggle.active span:nth-child(3) { transform: translateY(-7px) rotate(-45deg); }

  /* =====================================================
     MOBILE MENU
     ===================================================== */
  .mobile-menu {
    display: none;
    position: fixed;
    top: var(--site-header-offset, 120px);
    left: 0;
    right: 0;
    bottom: 0;
    background: #ffffff;
    overflow-y: auto;
    -webkit-overflow-scrolling: touch;
    overscroll-behavior: contain;
    z-index: 999;
    visibility: hidden;
    opacity: 0;
    pointer-events: none;
    transition: opacity .2s ease, visibility 0s linear .2s;
  }
  .mobile-menu.active {
    visibility: visible;
    opacity: 1;
    pointer-events: auto;
    transition: opacity .2s ease, visibility 0s linear 0s;
  }

  .mobile-menu__inner {
    padding: 12px 18px;
    padding-bottom: max(28px, env(safe-area-inset-bottom));
    min-height: 100%;
    display: flex;
    flex-direction: column;
  }

  .mobile-menu__list {
    list-style: none;
    padding: 0;
    margin: 0;
    flex: 1;
  }

  .mobile-menu__item { border-bottom: 1px solid #eef0f2; }
  .mobile-menu__item:last-child { border-bottom: none; }

  /* Row: enlace principal + botón flecha */
  .mobile-menu__row {
    display: flex;
    align-items: stretch;
    width: 100%;
  }
  .mobile-menu__row .mobile-menu__link--main { flex: 1; }

  .mobile-menu__link {
    display: flex;
    align-items: center;
    justify-content: space-between;
    width: 100%;
    padding: 16px 0;
    font-size: 1.05rem;
    font-weight: 500;
    color: #2a2a35;
    text-decoration: none;
    background: none;
    border: none;
    cursor: pointer;
    text-align: left;
    min-height: 52px;
  }
  .mobile-menu__link.active { color: #D1007A; }

  .mobile-menu__trigger {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 52px;
    min-height: 52px;
    padding: 0;
    background: none;
    border: none;
    color: #2a2a35;
    cursor: pointer;
    -webkit-tap-highlight-color: rgba(209, 0, 122, 0.10);
    touch-action: manipulation;
    transition: background-color .15s ease, color .15s ease;
  }
  /* Mobile: solo cambio de color, sin rotaciones */
  .mobile-menu__trigger svg { transition: color .15s ease; }
  .mobile-menu__item.open .mobile-menu__trigger {
    background: rgba(209, 0, 122, 0.08);
    color: #D1007A;
  }
  .mobile-menu__trigger:active { background: rgba(15, 15, 30, 0.04); }

  /* Submenú: display toggle, sin animaciones de altura */
  .mobile-menu__submenu {
    display: none;
    padding: 4px 0 12px;
  }
  .mobile-menu__item.open .mobile-menu__submenu { display: block; }

  .mobile-menu__group { margin-bottom: 10px; }
  .mobile-menu__group:last-child { margin-bottom: 0; }

  .mobile-menu__group-header {
    display: flex;
    align-items: center;
    gap: 10px;
    width: 100%;
    padding: 13px 14px;
    font-size: 0.78rem;
    font-weight: 700;
    color: #0f0f1e;
    background: #fafafc;
    border: none;
    border-left: 3px solid var(--column-color, #D1007A);
    border-radius: 0 8px 8px 0;
    cursor: pointer;
    text-align: left;
    min-height: 44px;
    text-transform: uppercase;
    letter-spacing: 1px;
    touch-action: manipulation;
    -webkit-tap-highlight-color: rgba(209, 0, 122, 0.10);
    transition: background-color .15s ease, color .15s ease;
  }
  .mobile-menu__group-icon {
    font-size: 0.95rem;
    flex-shrink: 0;
    width: 24px;
    height: 24px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    background: color-mix(in srgb, var(--column-color, #D1007A) 12%, #fff);
    border-radius: 5px;
  }
  .mobile-menu__group-title { flex: 1; }
  .mobile-menu__group-header svg {
    flex-shrink: 0;
    opacity: .55;
    transition: opacity .15s ease, color .15s ease;
  }
  .mobile-menu__group.open .mobile-menu__group-header {
    background: color-mix(in srgb, var(--column-color, #D1007A) 8%, #fff);
    color: var(--column-color, #D1007A);
  }
  .mobile-menu__group.open .mobile-menu__group-header svg { opacity: 1; }

  /* Lista de subitems: display toggle */
  .mobile-menu__group-list {
    display: none;
    list-style: none;
    padding: 6px 0 6px 28px;
    margin: 0;
  }
  .mobile-menu__group.open .mobile-menu__group-list { display: block; }

  .mobile-menu__sublink {
    display: block;
    padding: 11px 14px;
    font-size: 0.92rem;
    color: #4a4a55;
    text-decoration: none;
    border-radius: 7px;
    border-left: 2px solid transparent;
    min-height: 44px;
    transition: color .15s ease, background-color .15s ease, border-color .15s ease;
  }
  .mobile-menu__sublink:active {
    color: #D1007A;
    background: rgba(209, 0, 122, 0.06);
    border-left-color: #D1007A;
  }
  @media (hover: hover) {
    .mobile-menu__sublink:hover {
      color: #D1007A;
      background: rgba(209, 0, 122, 0.06);
      border-left-color: #D1007A;
    }
  }

  .mobile-menu__view-all {
    display: block;
    margin-top: 4px;
    padding: 11px 14px;
    font-size: 0.74rem;
    font-weight: 700;
    color: #D1007A;
    text-decoration: none;
    text-transform: uppercase;
    letter-spacing: 0.8px;
    min-height: 44px;
    border-top: 1px solid #f0f0f4;
    transition: background-color .15s ease;
  }
  .mobile-menu__view-all:active,
  .mobile-menu__view-all:hover { background: rgba(209, 0, 122, 0.06); }


  /* =====================================================
     RESPONSIVE
     ===================================================== */
  @media (max-width: 1380px) {
    .header__container { padding: 12px 24px; gap: 16px; }
  }

  @media (max-width: 1280px) {
    .header__menu { gap: 0; }
    .header__link { padding: 10px 10px; font-size: 0.86rem; }
    .header__mega-toggle { width: 22px; height: 22px; }
    .header__contact { padding: 3px; }
    .suc-chip { padding: 4px 8px 4px 5px; gap: 6px; }
    .suc-chip__icon { width: 24px; height: 24px; }
    .suc-chip__num { font-size: 0.8rem; }
    .suc-chip__city { font-size: 0.56rem; letter-spacing: 1.1px; }
    .header__cta { padding: 10px 16px; font-size: 0.85rem; }
  }

  @media (max-width: 1180px) {
    .suc-chip__city { display: none; }
    .suc-chip__num { font-size: 0.78rem; }
    .header__actions { gap: 8px; }
  }

  @media (max-width: 1280px) {
    .mega-menu__container { grid-auto-columns: 250px; }
    .mega-menu__column { padding: 22px 16px 0; }
    .mega-menu__view-all { padding: 12px 16px; width: calc(100% + 32px); margin-left: -16px; }
  }

  @media (max-width: 1100px) {
    .mega-menu__container { grid-auto-columns: 230px; }
    .header__contact { display: none; }
  }

  @media (max-width: 1024px) {
    .header__nav,
    .header__actions { display: none; }
    .header__toggle { display: flex; }
    .mobile-menu { display: block; }
    .header__container { padding: 12px 20px; min-height: 72px; }
    .header__logo img { height: 50px; }
  }

  @media (max-width: 768px) {
    .header__container { padding: 10px 16px; min-height: 64px; }
    .header__logo img { height: 44px; max-width: 200px; }
  }

  @media (max-width: 480px) {
    .header__container { padding: 10px 12px; min-height: 60px; }
    .header__logo img { height: 38px; max-width: 170px; }
    .mobile-menu__inner { padding: 16px; }
    .mobile-menu__link { padding: 14px 0; font-size: 1rem; }
    .mm-suc { padding: 12px; }
    .mm-suc__num { font-size: 1.05rem; }
  }

  /* Body scroll lock when menu is open */
  :global(body.menu-open) { overflow: hidden; }

  /* Reduced motion */
  @media (prefers-reduced-motion: reduce) {
    .header,
    .header__link,
    .header__link::before,
    .header__arrow,
    .mega-menu,
    .header__cta,
    .mobile-menu,
    .mobile-menu__submenu,
    .mobile-menu__group-list,
    .mobile-menu__trigger svg,
    .mobile-menu__group-header svg {
      transition: none !important;
    }
  }
</style>
